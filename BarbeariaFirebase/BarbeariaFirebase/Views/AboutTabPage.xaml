<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
            prism:ViewModelLocator.AutowireViewModel="True"
            x:Class="BarbeariaFirebase.Views.AboutTabPage"
            Title="{Binding Title}">
    <ContentPage.IconImageSource>
        <FontImageSource Glyph="info-circle" 
                                 FontFamily="{StaticResource FontAwesomeSolid}"
                                Color="White"/>
    </ContentPage.IconImageSource>
    <StackLayout>

        <!--Image Style="{DynamicResource Banner}" /-->
        <!--Label  Text="{Binding ConceptText}" /-->
        <Label  Style="{DynamicResource MainLabel}" Text="{Binding ConceptText}" />

    </StackLayout>
</ContentPage>